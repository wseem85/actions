name: test workflow

on: 
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a Welcome Message
        run: echo "Welcome to your first Github Action"

      - name: Ckeckout Repositery
        uses: actions/checkout@v6.0.2

      - name: List Repositery files
        run : ls -al
